---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart radio_bot
  ansible.builtin.systemd:
    name: "{{ radio_bot_identifier }}.service"
    state: restarted
    daemon_reload: true
  failed_when: false
  when: radio_bot_enabled | bool
